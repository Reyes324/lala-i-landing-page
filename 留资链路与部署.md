# 留资链路与部署运维

> 最后更新：2026-09-04
> 这份文档记录「表单提交 → 数据落到飞书」这条链路的现状，以及部署时踩过的坑。
> 改动留资功能或部署前先读这里。

## 一、三个地址的关系（最容易搞混的地方）

| 地址 | 是什么 | 谁在维护 |
|---|---|---|
| `https://lala-i-promotional.sg-van.lalamove.com/` | **用户实际在用的正式页面**，货拉拉自有域名，国内可访问 | 前端同事手动部署 |
| `https://lala-i-intro.netlify.app/` | 我们自己的预览站，同时**托管留资后端函数** | 本仓库 push 后自动构建 |
| `Reyes324/lala-i-landing-page`（GitHub main） | 源码仓库 | 本地 push |

**关键事实：改完代码推 GitHub，只会更新 Netlify 预览站，正式页面不会变。**
正式页面必须把 `index.html` + `assets/` 打包发给前端同事重新部署一次。
判断依据：飞书留资表里「来源页面」列记录的全是 `sg-van.lalamove.com` 那个域名。

## 二、留资链路现状

```
表单提交 → fetch(LEAD_API) → Netlify Function → 飞书多维表格「lala-i 官网留资」
```

- 前端：`index.html` 里搜 `LEAD_API`，当前值
  `https://lala-i-intro.netlify.app/.netlify/functions/lead`
  **写绝对地址不写相对路径**——因为这个页面会被部署到多个不同域名下，相对路径会 404。
- 后端：`netlify/functions/lead.js`
- 目标表：https://huolala.feishu.cn/base/CkT1bBgK3aMwKKsDcNec2mQtnTf

### Netlify 环境变量

在 Netlify → Site configuration → Environment variables 配置。**值不写在这里，代码和文档里只出现变量名。**

| 变量 | 说明 |
|---|---|
| `LEADS_APP_ID` | 飞书自建应用 App ID（`cli_a940e9e9bbb99bd2`） |
| `LEADS_APP_SECRET` | 对应 Secret，已标记为 Netlify secret（只写不读） |
| `LEADS_BASE_TOKEN` | `CkT1bBgK3aMwKKsDcNec2mQtnTf` |
| `LEADS_TABLE_ID` | `tblCRvuI4cTIzlVQ` |
| `ALLOWED_ORIGINS` | 可选，未配置 → CORS 放开 `*`。因为页面会部署到多个域名，暂时保持放开 |

用独立的 `LEADS_*` 前缀，是为了**不和批注功能 `annotate.js` 用的 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 冲突**（那是另一条链路、另一张 Base，别混）。`lead.js` 在 `LEADS_*` 缺失时会回退到 `FEISHU_*`。

## 三、为什么从 Vercel 迁到 Netlify（2026-09-04）

原来后端是独立的 Vercel 函数 `lead-api-six.vercel.app`。**`*.vercel.app` 在中国大陆被阻断**，用户不挂 VPN 时表单提交必然失败。

典型症状：**页面能正常打开、一点提交就报错**——因为页面在 Netlify（国内能通）、接口在 Vercel（国内不通），两个域名可达性不一样。

迁移后只剩 Netlify 一个境外域名，页面能打开就说明接口也能通。

> ⚠️ `netlify.app` 目前国内可访问，但这是「目前能通」，不保证长期稳定。
> **最终正解**：让货拉拉后端在 `sg-van.lalamove.com` 同域下提供 `/api/lead`，
> 逻辑照搬 `netlify/functions/lead.js` 即可（就是拿参数写飞书多维表格）。
> 真做了的话，前端把 `LEAD_API` 改成 `/api/lead` 就行。

## 四、部署踩坑：Netlify 自动发布是锁住的

**push 后会自动构建，但不会自动发布。** 2026-09-04 遇到：新 deploy 构建成功、状态 ready、函数也打包进去了，但站点的 published deploy 还停在旧 commit，导致新函数一直 404。

所以**不能只看构建成功就认为线上生效了**，要确认 published deploy 的 commit 跟上了 HEAD。

```bash
netlify link --name lala-i-intro       # 首次需要，会生成 .netlify/state.json（已 gitignore）
SID=$(python3 -c "import json;print(json.load(open('.netlify/state.json'))['siteId'])")

# 看线上发布的到底是哪个 commit
netlify api getSite --data "{\"site_id\":\"$SID\"}" | python3 -c "
import sys,json; d=json.load(sys.stdin); pd=d['published_deploy']
print(pd['commit_ref'][:7], pd['state'], [f['n'] for f in pd.get('available_functions') or []])"

# 和 git rev-parse --short HEAD 对不上时，手动发布最新的那个 deploy：
netlify api listSiteDeploys --data "{\"site_id\":\"$SID\"}"   # 拿最新 deploy 的 id
netlify api unlockDeploy    --data "{\"deploy_id\":\"<当前published的id>\"}"
netlify api restoreSiteDeploy --data "{\"site_id\":\"$SID\",\"deploy_id\":\"<新deploy的id>\"}"
```

## 五、验证方法

改完之后必须实测，不要只看部署成功：

```bash
# 1. 函数活着
curl -o /dev/null -w "%{http_code}\n" -X OPTIONS \
  https://lala-i-intro.netlify.app/.netlify/functions/lead     # 期望 204

# 2. 端到端写入
curl -X POST https://lala-i-intro.netlify.app/.netlify/functions/lead \
  -H "Content-Type: application/json" \
  -d '{"contact":"测试-勿跟进","contactType":"微信号","channel":"smoke-test"}'
                                                              # 期望 {"ok":true}

# 3. 线上页面拿到的确实是新地址
curl -s https://lala-i-intro.netlify.app/ | grep -o "const LEAD_API = '[^']*'"
```

**测完记得去飞书表里删掉测试记录**，否则会混进真实线索：

```bash
lark-cli base +record-list   --base-token CkT1bBgK3aMwKKsDcNec2mQtnTf --table-id tblCRvuI4cTIzlVQ
lark-cli base +record-delete --base-token CkT1bBgK3aMwKKsDcNec2mQtnTf --table-id tblCRvuI4cTIzlVQ \
  --record-id <recXXXX> --yes
```

注意 lark-cli 用的是 `--base-token` 不是 `--app-token`。

最后还要**不挂 VPN**在正式域名上手动提交一次，这才是用户的真实路径。

## 六、交付给前端同事

只发两样东西 + 一份说明，他那边零配置：

```
lala-i-landing-page/
├── index.html
├── assets/
└── UPDATE-NOTES.md
```

打包命令（内部文件名用英文，避免 Windows 解压乱码）：

```bash
cd ~/Desktop/lala-i官网
STG=/tmp/deliver && rm -rf $STG && mkdir -p $STG/lala-i-landing-page
cp index.html $STG/lala-i-landing-page/ && cp -R assets $STG/lala-i-landing-page/
find $STG -name .DS_Store -delete
cd $STG && zip -rqX ~/Desktop/lala-i官网-交付-$(date +%Y%m%d).zip lala-i-landing-page
```

**不要**把 `lead-api/`、`netlify/`、各种 `.md` 打进去——那是我们的后端和内部文档。

## 七、历史遗留

`lead-api/` 是旧的 Vercel 版本，逻辑和 `netlify/functions/lead.js` 一样，暂时保留未删。
等正式域名部署新版、确认稳定跑一段时间后，可以删掉这个目录并在 Vercel 后台下线项目。
