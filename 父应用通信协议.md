# 父应用 iframe 通信协议

本站（lala-i-intro，静态介绍页）被父应用以 iframe 嵌入，不做登录/下单等业务，仅展示 + 按此协议与父应用通信。协议由前端同学定义，父应用负责实现监听与路由跳转；本站只负责按规则发消息。

## 1. 父应用打开本站的 URL 约定

```
https://lala-i-intro.netlify.app/?env=dev&loggedIn=0
https://lala-i-intro.netlify.app/?env=pre&loggedIn=1
https://lala-i-intro.netlify.app/?env=prd&loggedIn=1
```

| 参数 | 含义 | 取值 |
|---|---|---|
| `env` | 父应用环境 | `dev` / `pre` / `prd` |
| `loggedIn` | 登录展示态（仅用于切换展示，不是真实鉴权，不接收 token） | `1` 已登录；`0` 或缺失/非法 → 未登录 |

## 2. env → 父应用 origin 映射表（postMessage 的唯一真相源）

```js
const PARENT_ORIGIN_BY_ENV = {
  dev: 'http://localhost:3001',
  pre: 'https://lala-i-web--stable-1.sg-van-pre.lalamove.com',
  prd: 'https://lala-i.net',
}
function resolveParentOrigin(env) {
  return PARENT_ORIGIN_BY_ENV[env] ?? null
}
```

规则：
- **只认这张表**，绝不从 URL 直接接收 parentOrigin 字符串当 target（防篡改攻击）
- `env` 缺失或非法 → 不发送 postMessage，只 `console.warn`
- 父域名以后变了，只改这张表，父应用继续传 `env` 即可
- origin 不带尾部 `/`

## 3. 统一发送函数

```js
function postNavigateToParent(type) {
  const params = new URLSearchParams(window.location.search)
  const parentOrigin = resolveParentOrigin(params.get('env'))
  if (!parentOrigin) {
    console.warn('[intro] invalid or missing env, skip postMessage', params.get('env'))
    return
  }
  window.parent.postMessage(
    { source: 'lala-i-intro', type },
    parentOrigin,
  )
}
```

消息字段固定：
- `source`：固定字符串 `'lala-i-intro'`
- `type`：下方白名单之一
- 暂不需要 `payload`

**禁止**：`window.open` / `location.href` / 改 `top.location` 自己跳父路由；`targetOrigin` 禁止用 `'*'`。

## 4. type 白名单（父应用已支持）

| 按钮意图 | type | 父应用跳转 | 未登录时父应用行为 |
|---|---|---|---|
| 去登录 | `GO_LOGIN` | `/login` | `/login` |
| 去估价 / 试用询价 | `GO_TRIAL_QUOTE` | `/trial-quote` | 可直接进 |
| 去下单 | `GO_BOOKING` | `/booking` | 改去 `/login` |
| 去 App 下载指引 | `GO_APP_DOWNLOAD` | 父应用自行处理（下载指引页已独立部署在 `lala-i-app.netlify.app`） | 前端同学 2026-08-27 提供，父应用已实现监听 |

新增 type 需要先跟前端同学确认父应用那边已经实现对应监听和路由，不能本站单方面新增。

## 5. 本站当前接入情况（index.html）

- header「登录」按钮（`#loginBtn`）→ `postNavigateToParent('GO_LOGIN')`；`loggedIn=1` 时隐藏该按钮
- 全站「立即试用」按钮（`[data-nav="GO_TRIAL_QUOTE"]`，共 5 处：header / Hero / CTA 区 / footer / 移动端底部条）→ `postNavigateToParent('GO_TRIAL_QUOTE')`
- `GO_BOOKING`（去下单）尚未接入任何按钮，白名单已支持，后续如需加"立即下单"类按钮直接复用 `postNavigateToParent('GO_BOOKING')` + `data-nav="GO_BOOKING"` 即可
- "下载 App" 板块「查看详细安装指引」按钮（`[data-nav="GO_APP_DOWNLOAD"]`）→ 嵌入场景走 `postNavigateToParent('GO_APP_DOWNLOAD')`；独立访问（未被 iframe 嵌入）时兜底直接跳转 `https://lala-i-app.netlify.app/`（见 `navigateOrPost` 里的 `STANDALONE_ROUTE_BY_TYPE`）
- 页内原有的三步询价表单区块（`#quote`）目前没有按钮可跳转到，处于孤儿状态（保留未删除）
