# 货拉拉企业国际版（lala-i）官网落地页 · 项目说明

## 一、项目背景来源

1. **飞书内容收集表**：https://huolala.feishu.cn/docx/OgHqdUKqRo2e1Lxng74cDQKenjb
   《东南亚企业用车官网｜内容收集表》，产品方已填写的"必填信息"，包括品牌定位、服务范围、核心能力、试用估价功能说明、信任信息授权（客户案例可用示例数据）、品牌视觉资产（主色 #2257D4、字体 Noto Sans SC + Inter）、首期页面栏目范围。

2. **本地项目背景**：`~/Desktop/claude 项目/企业国际版/`
   已有的 lala-i Web App demo（PC 端货运下单应用），其中的设计系统主蓝色 `#2257D4` 与飞书文档给出的品牌主色完全一致，账号体系设计（子母账号、E001-VN 这类 ID 格式）也是复用同一份产品决策记忆（见 `enterprise-international.md`）。

**结论**：官网首页需要传达的品牌信息、卖点、账号体系概念，和已有 Web App 是同一套产品语言，本次官网只是"对外获客"入口，不改变任何产品事实。

## 二、本次工作方法

1. 用 `lark-doc` skill 读取飞书内容收集表全文（含图片说明文字），提取必填信息。
2. 读取本地 `enterprise-international.md` 记忆 + 项目目录下的 `README.md`，交叉验证品牌色、账号体系表述是否一致。
3. 筛选信息缺口：客户案例、数据证据、联系方式等真实数据缺失部分，飞书文档已明确授权"可用示例数据"，因此不阻塞第一版设计。
4. **v1（`~/Desktop/claude 项目/企业国际版/官网/index.html`，已同步复制到本文件夹 `index.html`）**：
   调用 `frontend-design` skill 承担视觉与信息架构实现，Prompt 中手动写入了 `landing-page-design` skill 的方法论要点（首屏公式、九段式结构、CTA 文案规范等）作为约束条件，而非直接触发该 skill 本身。
5. **v2（`v2-skill版.html`）**：应用户要求，**直接调用 `landing-page-design` skill**，严格按其方法论重做一版，刻意在关键处与 v1 做出差异化，体现"更贴近 skill 原文"的设计取舍（见下文对比）。
6. 每个版本都用 Playwright 实际起浏览器做了视觉验证（桌面 1440px + 移动 390px），过程中发现并修复了 3 个真实 bug：
   - 导航文字在深色首屏背景下对比度不足（v1）
   - 移动端 Logo 文字换行撑高头部、和正文重叠（v1）
   - 首屏浮动徽章遮挡"实时报价"文字（v1）

## 三、v1 与 v2 的关键差异（为什么两版长得不一样）

| 维度 | v1（frontend-design 主导） | v2（landing-page-design skill 严格版） |
|---|---|---|
| Hero 视觉 | "试用估价" 产品界面 mockup（选类型/填地点/出价流程可视化） | **Before / After 对比图**：左边"以前"是分散的车队标签+人工对账表，右边"现在"是一个绿色对勾+统一账户结论。更贴近 skill 里"Hero image 应展示结果（outcome），不是产品截图"的硬性规则——v1 的产品 mockup 其实正好踩了 skill《Common Mistakes》里点名的坑 |
| 标题 | "东南亚七国货运，一个账户全搞定" | **严格套用 `[Outcome] without [pain]` 公式**："东南亚准时送达，不必自建车队" |
| CTA 文案 | "免费试用估价" | "免费查看运费，无需注册"（动词+价值+降低顾虑，对应 skill 的 CTA 公式） |
| 页面结构 | 12 个板块（额外加了数据条、覆盖城市地图、开发者/API 独立板块），信息更全 | **严格 9 段式**（Hero→社交证明→痛点→方案→流程→客户故事→Pricing→FAQ→最终CTA），覆盖城市/API 能力收敛进"方案"三大支柱里，更精简 |
| 功能卡片数量 | 6 个并列功能卡 | 收敛成 **3 个支柱**（每个支柱下 3 条要点），对应 skill "3 key features" 的建议 |
| 表单 | 无独立表单，"立即下单"走的是产品内动作 | 新增**预约演示表单**，只留姓名+邮箱两个字段，单列布局，对应 skill 的 Form Design 规则（每多一个字段转化掉 ~11%） |
| 结尾 CTA | 无明确时效紧迫感 | 加了"今天申请，最快当天开通"制造紧迫感（对应 skill《Common Mistakes》"无紧迫感"这条） |

两版共享同一套视觉语言（深藏青底 + 品牌蓝 `#2257D4` + Noto Sans SC/Inter），只是信息架构的取舍不同：v1 更像"官网首页"（信息量大、栏目全），v2 更像"教科书式转化落地页"（结构紧、单一转化路径清晰）。

## 四、内容与文案来源说明

以下内容飞书文档中**已明确授权使用示例数据**，非真实数据，页面上均标注"示例"：
- 客户 logo 墙（CaiNiao Global / JD Logistics / JT Express / FreshChain）
- 客户故事 3 条引语（跨境电商仓配团队 / 生鲜出海连锁品牌 / 3C 出海品牌）
- 联系方式（企业热线、邮箱、企业微信二维码）

以下内容来自飞书文档的真实产品信息，非虚构：
- 覆盖 7 国/地区：泰国、越南、马来西亚、印尼、菲律宾、新加坡、香港
- 车型范围：二轮摩托 → 面包车 → 卡车 → 厢式/开篷货车 → 9.6 米大车
- 结算方式：人民币统一结算（月末中行汇率对账，次月月末还账单）、充值 + 账期
- 企业子母账号（如 E001-VN、E001-TH 格式）
- 24 小时双语客服、API 对接
- "试用估价"功能已上线

## 五、待你确认 / 飞书文档里的原始留白

这些是文档里明确没人填的部分，会影响后续迭代方向：
- 不希望出现的表达 / 竞品联想（完全空白）
- 上线时间、技术限制、是否需要多语言（完全空白）
- 真实的客户案例、资质认证、媒体报道（暂无，当前用示例占位）

## 六、文件清单

- `index.html` —— v1，frontend-design 版（完整信息架构，12 板块）
- `v2-skill版.html` —— v2，landing-page-design skill 严格版（精简 9 段式 + 转化表单）
- 原始 v1 文件同时保留在 `~/Desktop/claude 项目/企业国际版/官网/index.html`（与本文件夹的 `index.html` 内容一致）

## 七、图标使用规范

页面里所有功能性/示意性 icon（痛点卡、功能支柱、API 能力卡、对勾等），**默认统一从 Lucide 取用**：https://lucide.dev/

- 官方原始 SVG 地址：`https://unpkg.com/lucide-static@latest/icons/<icon-name>.svg`（`<icon-name>` 从 lucide.dev 官网搜索/复制）
- 保留原始 `viewBox="0 0 24 24"`、`stroke="currentColor"`、`stroke-width="2"`、`stroke-linecap="round"`、`stroke-linejoin="round"` 属性，只按容器需要调整 `width`/`height`（本页痛点卡用 20px，功能支柱/API 卡用 22px）
- 找不到语义完全对应的图标时，选一个视觉简洁、含义相近的替代（例如"人民币结算"没有专门的元符号图标，用 `banknote` 代替），不要退回手绘 SVG
- 之前一版曾用过 Tabler Icons（tabler.io/icons），现已全部替换为 Lucide；后续新增图标一律直接查 Lucide，不再混用其他图标库
- **备选库**：Lucide 找不到合适图标时，可以去 Remix Icon（https://remixicon.com/）挑，风格上是线性图标，和 Lucide 同属简洁描边风格，混用违和感较小

## 八、字重体系（踩过的坑：Noto Sans SC 字重必须和 Google Fonts 加载列表完全对应）

**根因排查记录**：中文字体 Noto Sans SC 一度只加载了 `400;500;700;900` 四档，但 CSS 里却有多处对中文文本写了 `font-weight:600`（表单 label、二维码卡片标题等）。浏览器在请求的字重未被加载时会向上取整匹配到最近的已加载字重（600→700），导致这些"本该是 600"的地方实际渲染成了 700，看起来跟标题一样粗——这是"字体太粗"反复被吐槽的根本原因，不是主观审美问题。

**修复**：Google Fonts 链接已改为 `Noto+Sans+SC:wght@400;500;600;700;900`，600 现在是真实加载的字重。

**当前字重分级**（中文文本为主，`--font-sc`/body 默认字体走 Noto Sans SC 回退）：
- **700**：仅 Hero 大标题（`.hero h1`）、主按钮 `.btn`/`.btn-text`、Pricing "推荐"渐变角标 `.rec-tag`——全站最强调的极少数元素
- **600**：`h1,h2,h3`（章节标题、卡片标题）、`.logo`、`.faq-q`（FAQ 问题行）、`.market-card strong`、`.fleet-item span`、`.field label`、二维码卡片标题
- **500**：`.tag`/`.hero-eyebrow`（小标签）、`.sample-tag`（"示例数据"免责标注）、`.photo-credit`、`.sp-stat span`、`.trust-logos .wordmark`
- **未设置（继承 400）**：所有正文段落、卡片描述文字、副标题
- 例外：`.sp-stat strong`、`.step3 .snum` 用 `--font-en`（Inter，本来就完整加载了 400–800），可以保持 800 做大数字强调，不受这条规则约束

**以后新增文字样式的铁律**：凡是中文文本要用某个字重，先确认 Google Fonts 链接里 Noto Sans SC 的 `wght@` 列表已经包含这个数字，没有就先加进去，不要凭感觉写 `font-weight` 却不检查是否真的被加载。
